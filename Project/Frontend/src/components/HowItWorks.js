import React from 'react';

export default function HowItWorks() {
  return (
    <section className="howitworks-section">
      <h2>How It Works</h2>
      <p>Farmers add product data → Distributors & Retailers update → Smart Contracts enforce rules → Consumers verify via QR codes.</p>
    </section>
  );
}
